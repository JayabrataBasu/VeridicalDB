run:
  skip-dirs: ["build", "data", ".github"]

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gofmt
    - gosimple
    - unused

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
